Crear SpriteTexture
Abre:

src/texture.rs


Actualmente tienes al comienzo:

pub struct Texture {
    width: usize,
    height: usize,
    pixels: Vec<u32>,
}

pub struct TextureSet {


Reemplázalo por:

pub struct Texture {
    width: usize,
    height: usize,
    pixels: Vec<u32>,
}

pub struct SpriteTexture {
    width: usize,
    height: usize,
    pixels: Vec<u32>,
}

pub struct TextureSet {


La textura de paredes seguirá utilizando RGB. El sprite tendrá su propia estructura porque necesita RGBA.

---

Implementar la carga RGBA
En src/texture.rs, busca el final de:

impl Texture {


Justo antes de:

impl TextureSet {


agrega:
 (1/8)
impl SpriteTexture {
    pub fn from_file(
        path: &str,
    ) -> Result<Self, String> {
        let image =
            ImageReader::open(
                path,
            )
            .map_err(
                |error| {
                    format!(
                        "No se pudo abrir el sprite '{path}': {error}"
                    )
                },
            )?
            .decode()
            .map_err(
                |error| {
                    format!(
                        "No se pudo decodificar el sprite '{path}': {error}"
                    )
                },
            )?
            .to_rgba8();

        let width =
            image.width()
                as usize;

        let height =
            image.height()
                as usize;

        if width == 0
            || height == 0
        {
            return Err(
                format!(
                    "El sprite '{path}' no puede estar vacío"
                ),
            );
        }

        let pixels =
            image
                .pixels()
                .map(
                    |pixel| {
                        let [
                            red,
                            green,
                            blue,
                            alpha,
                        ] = pixel.0;

                        (
                            (alpha as u32)
                                << 24
                        )
                            | (
                                (red as u32)
                                    << 16
                            )
                            | (
                                (green as u32)
                                    << 8
                            )
                            | blue as u32
                    },
                )
                .collect();

        Ok(
            Self {
                width,
                height,
                pixels,
 (2/8)
},
        )
    }

    pub fn width(
        &self,
    ) -> usize {
        self.width
    }

    pub fn height(
        &self,
    ) -> usize {
        self.height
    }

    pub fn sample(
        &self,
        texture_x: usize,
        texture_y: usize,
    ) -> u32 {
        self.pixels[
            texture_y
                * self.width
                + texture_x
        ]
    }
}


Después de cargo fmt aparecerá mucho más compacto. Es correcto.

Cada píxel se almacena como:

0xAARRGGBB


Esto permite extraer:

alpha = pixel >> 24;
color = pixel & 0x00FF_FFFF;


---

Agregar la prueba de transparencia
En el módulo de tests de src/texture.rs, busca:

mod tests {
    use super::TextureSet;


Reemplázalo por:

mod tests {
    use super::{
        SpriteTexture,
        TextureSet,
    };


Antes de la prueba:

fn selects_a_stable_wall_texture_for_each_level()


agrega:

#[test]
fn loads_sprite_transparency() {
    let sprite =
        SpriteTexture::from_file(
            "./assets/sprites/test_entity.png",
        )
        .expect(
            "El sprite de prueba debe cargar correctamente",
        );

    assert_eq!(
        sprite.width(),
        64,
    );

    assert_eq!(
        sprite.height(),
        96,
    );

    assert_eq!(
        sprite.sample(
            0,
            0,
        ) >> 24,
        0,
    );

    assert_eq!(
        sprite.sample(
            31,
            17,
        ) >> 24,
        255,
    );
}


La prueba confirma:

dimensiones correctas
esquina transparente
cabeza opaca


---

Importar SpriteTexture en el renderer
Abre:

src/renderer.rs


Busca:

use crate::texture::TextureSet;


Reemplázalo por:

use crate::texture::{
    SpriteTexture,
    TextureSet,
};


---

Retirar el color sólido de WorldSprite
Busca:

pub struct WorldSprite {
    pub x: f32,
 (3/8)
pub y: f32,
    pub size: f32,
    pub color: u32,
}


Reemplázalo por:

pub struct WorldSprite {
    pub x: f32,
    pub y: f32,
    pub size: f32,
}


Ahora el color vendrá del PNG.

---

Recibir la textura en render_test_sprite
Busca la firma:

pub fn render_test_sprite(
    framebuffer: &mut Framebuffer,
    depth_buffer: &[f32],
    player: &Player,
    sprite: &WorldSprite,
) {


Reemplázala por:

pub fn render_test_sprite(
    framebuffer: &mut Framebuffer,
    depth_buffer: &[f32],
    player: &Player,
    sprite: &WorldSprite,
    texture: &SpriteTexture,
) {


---

Conservar la proporción del PNG
Dentro de render_test_sprite, busca:

let projected_size =
    sprite.size
        / perpendicular_distance
        * projection_distance;

let left =
    (
        screen_center_x
            - projected_size / 2.0
    )
    .floor()
        as i32;

let right =
    (
        screen_center_x
            + projected_size / 2.0
    )
    .ceil()
        as i32;

let top =
    (
        height as f32 / 2.0
            - projected_size / 2.0
    )
    .floor()
        as i32;

let bottom =
    (
        height as f32 / 2.0
            + projected_size / 2.0
    )
    .ceil()
        as i32;


Reemplázalo por:

let projected_height =
    sprite.size
        / perpendicular_distance
        * projection_distance;

let projected_width =
    projected_height
        * texture.width()
            as f32
        / texture.height()
            as f32;

let left =
    (
        screen_center_x
            - projected_width / 2.0
    )
    .floor()
        as i32;

let right =
    (
        screen_center_x
            + projected_width / 2.0
    )
    .ceil()
        as i32;

let top =
    (
        height as f32 / 2.0
            - projected_height / 2.0
    )
    .floor()
        as i32;

let bottom =
    (
        height as f32 / 2.0
            + projected_height / 2.0
    )
 (4/8)
.ceil()
        as i32;


Esto evita que el PNG 64×96 se deforme como un cuadrado.

---

Reemplazar el relleno magenta por muestreo RGBA
Al final de render_test_sprite, actualmente tienes:

for x in
    left_clamped
        ..=right_clamped as usize
{
    if perpendicular_distance
        >= depth_buffer[x]
    {
        continue;
    }

    for y in
        top_clamped
            ..=bottom_clamped
                as usize
    {
        buffer[
            y * width + x
        ] = sprite.color;
    }
}


Reemplázalo completamente por:

for x in
    left_clamped
        ..=right_clamped
            as usize
{
    if perpendicular_distance
        >= depth_buffer[x]
    {
        continue;
    }

    let texture_x =
        (
            (
                x as f32
                    - left as f32
            )
                / projected_width
                * texture.width()
                    as f32
        ) as usize;

    let texture_x =
        texture_x.min(
            texture.width() - 1,
        );

    for y in
        top_clamped
            ..=bottom_clamped
                as usize
    {
        let texture_y =
            (
                (
                    y as f32
                        - top as f32
                )
                    / projected_height
                    * texture.height()
                        as f32
            ) as usize;

        let texture_y =
            texture_y.min(
                texture.height() - 1,
            );

        let pixel =
            texture.sample(
                texture_x,
                texture_y,
            );

        let alpha =
            pixel >> 24;

        if alpha == 0 {
            continue;
        }

        let color =
            pixel
                & 0x00FF_FFFF;

        if alpha == 255 {
            buffer[
                y * width + x
            ] = color;

            continue;
        }
 (5/8)
let background =
            buffer[
                y * width + x
            ];

        let inverse_alpha =
            255 - alpha;

        let red =
            (
                (
                    (color >> 16)
                        & 0xFF
                ) * alpha
                    + (
                        (
                            background
                                >> 16
                        ) & 0xFF
                    ) * inverse_alpha
            ) / 255;

        let green =
            (
                (
                    (color >> 8)
                        & 0xFF
                ) * alpha
                    + (
                        (
                            background
                                >> 8
                        ) & 0xFF
                    ) * inverse_alpha
            ) / 255;

        let blue =
            (
                (
                    color
                        & 0xFF
                ) * alpha
                    + (
                        background
                            & 0xFF
                    ) * inverse_alpha
            ) / 255;

        buffer[
            y * width + x
        ] =
            (
                red << 16
            )
                | (
                    green << 8
                )
                | blue;
    }
}


Aunque el PNG diagnóstico usa principalmente alfa 0 o 255, dejamos soporte para píxeles semitransparentes. Eso servirá más adelante para bordes suavizados.

La ruta rápida es:

if alpha == 255 {
    buffer[...] = color;
    continue;
}


Así que los píxeles completamente opacos no realizan mezcla RGB innecesaria.

---

Importar y cargar el PNG en main.rs
Abre:

src/main.rs


Busca:

use crate::texture::TextureSet;


Reemplázalo por:

use crate::texture::{
    SpriteTexture,
    TextureSet,
};


Ahora busca el sprite:

let test_sprite =
 (6/8)
WorldSprite {
        x: 550.0,
        y: 150.0,
        size: 80.0,
        color: 0xFF00FF,
    };


Reemplázalo por:

let test_sprite =
    WorldSprite {
        x: 550.0,
        y: 150.0,
        size: 80.0,
    };

let test_sprite_texture =
    SpriteTexture::from_file(
        "./assets/sprites/test_entity.png",
    )
    .unwrap_or_else(
        |error| {
            panic!(
                "{error}"
            )
        },
    );


Después de cargo fmt probablemente quedará así:

let test_sprite = WorldSprite {
    x: 550.0,
    y: 150.0,
    size: 80.0,
};

let test_sprite_texture =
    SpriteTexture::from_file("./assets/sprites/test_entity.png")
        .unwrap_or_else(|error| panic!("{error}"));


La imagen se carga una sola vez durante el inicio. Nunca por frame.

---

Pasar la textura al renderer
Busca:

render_test_sprite(
    &mut framebuffer,
    &depth_buffer,
    &player,
    &test_sprite,
);


Reemplázalo por:

render_test_sprite(
    &mut framebuffer,
    &depth_buffer,
    &player,
    &test_sprite,
    &test_sprite_texture,
);


---